# Show readable stack traces on errors. Never enable on a public site.
APP_DEBUG=false

# Set to false on Apache hosts without mod_rewrite; links become index.php?route=...
APP_PRETTY_URLS=true

# SQLite database file. Relative paths resolve from the project root.
DB_PATH=storage/database.sqlite
